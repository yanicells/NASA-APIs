<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>NASA Mars Rover</title>
    <link rel="icon" type="image/png" href="/images/logo.png">
</head>

<body class="bg-gray-900 min-h-screen flex flex-col">
    <%- include("partials/header.ejs") %>

    <main class="flex-1 px-4 py-8">
        <div class="text-center mb-12">
            <h1 class="text-5xl md:text-6xl font-bold text-white mb-4">
                Mars Rover 
                <span class="bg-gradient-to-r from-red-400 to-orange-500 bg-clip-text text-transparent">
                    Explorer
                </span>
            </h1>
        </div>

        <!-- Photos Gallery -->
        <% if(locals.photos) { %>
            <div class="mb-16">
                <% if (locals.photos.length > 0) { %>
                    <h2 class="text-3xl font-bold text-white text-center mb-8">Results for <%= locals.name %> on <%= locals.date %>
                    </h2>
                <% } else { %>
                <h2 class="text-3xl font-bold text-white text-center mb-8">No results found...</h2>
                <div class="max-w-md mx-auto mt-6">
                    <div class="bg-gradient-to-br from-gray-800 to-gray-900 p-8 rounded-2xl shadow-2xl border border-gray-700">
                        <h2 class="text-xl font-bold text-white mb-6 text-center">Available Mars Rover Photo Dates</h2>
                        <ul class="grid grid-cols-3 gap-3 text-center">
                        <% for (let i=0; i < Math.min(locals.dates.length, 15); i++) { %>
                        <li class="bg-red-600/20 text-red-300 border border-red-500/30 rounded-lg py-2 px-3 text-sm cursor-pointer hover:bg-red-600/30 hover:text-red-200 transition-all duration-200">
                          <%= locals.dates[i] %>
                        </li>
                        <% } %>
                        </ul>
                        <p class="text-gray-400 text-sm text-center mt-4">
                            Try selecting one of these available dates
                        </p>
                    </div>
                </div>

                <% } %>
                <div class="max-w-6xl mx-auto">
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        <% for (let i = 0; i < Math.min(locals.photos.length, 15); i++) { %>
                        <div class="group relative overflow-hidden rounded-2xl bg-gray-800 shadow-lg hover:shadow-2xl transition-all duration-300">
                            <div class="aspect-square overflow-hidden">
                                <img class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300" 
                                     src="<%= locals.photos[i].img_src %>" 
                                     alt="Mars Rover Photo <%= i + 1 %>" />
                            </div>
                            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                                <div class="absolute bottom-4 left-4 text-white">
                                    <p class="text-sm font-medium">Photo <%= i + 1 %></p>
                                    <p class="text-xs text-gray-300">Mars Surface</p>
                                </div>
                            </div>
                        </div>
                        <% } %>
                    </div>
                    
                    <% if(locals.photos.length > 15) { %>
                    <div class="text-center mt-8">
                        <p class="text-gray-400">Showing 15 of <%= locals.photos.length %> photos</p>
                    </div>
                    <% } %>
                </div>
            </div>
        <% } %>

        <div class="flex justify-center mb-16">
            <div class="bg-gradient-to-br from-gray-800 to-gray-900 p-8 rounded-2xl shadow-2xl w-full max-w-md border border-gray-700">
                <h2 class="text-2xl font-bold text-white mb-6 text-center">
                    Explore Mars Photos
                </h2>
                
                <form action="/mars-rover" method="POST" class="space-y-6">
                    <div>
                        <label for="rover" class="block mb-2 text-sm font-semibold text-gray-300">
                            Choose Rover
                        </label>
                        <select name="rover" id="rover" 
                                class="w-full p-3 rounded-lg bg-gray-700 text-white border-2 border-gray-600 focus:border-red-500 focus:ring-2 focus:ring-red-500/20 transition-all">
                            <option value="curiosity">Curiosity</option>
                            <option value="opportunity">Opportunity</option>
                            <option value="spirit">Spirit</option>
                            <option value="perseverance">Perseverance</option>
                        </select>
                    </div>

                    <div>
                        <label for="dateInput" class="block mb-2 text-sm font-semibold text-gray-300">
                            Select Date
                        </label>
                        <input type="date" 
                               id="dateInput" 
                               name="selectedDate"
                               class="w-full p-3 rounded-lg bg-gray-700 text-white border-2 border-gray-600 focus:border-red-500 focus:ring-2 focus:ring-red-500/20 transition-all">
                    </div>
                    
                    <button type="submit"
                            class="w-full bg-gradient-to-r from-red-600 to-orange-600 hover:from-red-700 hover:to-orange-700 text-white font-bold py-4 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
                        Launch Search
                    </button>
                </form>
            </div>
        </div>
    </main>
</body>
<%- include("partials/footer.ejs") %>
</html>
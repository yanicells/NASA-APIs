<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>NASA Mars Weather</title>
    <link rel="icon" type="image/png" href="/images/logo.png">
</head>

<body class="bg-gray-900 min-h-screen flex flex-col">
    <%- include("partials/header.ejs") %>

        <main class="flex-1 px-4 py-8">
            <div class="text-center mb-12">
                <h1 class="text-5xl md:text-6xl font-bold text-white mb-4">
                    Mars Weather
                    <span class="bg-gradient-to-r from-cyan-600 to-teal-500 bg-clip-text text-transparent">
                        Data
                    </span>
                </h1>
            </div>

            <div class="flex justify-center mb-16 <%= locals.sols ? 'hidden' : '' %>">
                <div
                    class="bg-gradient-to-br from-gray-800 to-gray-900 p-8 rounded-2xl shadow-2xl w-full max-w-md border border-gray-700">
                    <h2 class="text-2xl font-bold text-white mb-6 text-center">
                        Explore NASA's Library
                    </h2>
                    <form action="/mars-weather" method="POST" class="space-y-6">
                        <button type="submit"
                            class="w-full bg-gradient-to-r from-cyan-600 to-teal-500 hover:from-cyan-700 hover:to-teal-600 text-white font-bold py-4 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-cyan-400/30">
                            Fetch Latest Weather
                        </button>
                    </form>
                </div>
            </div>

            <div class="max-w-7xl mx-auto px-4">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <% if (locals.sols) { %>
                        <% for (var i=0; i < locals.sols.length; i++) { const sol=locals.sols[i]; const solData=locals.weather[sol];
                            %>
                            <div
                                class="bg-gradient-to-br from-cyan-900/60 to-teal-800/70 border border-cyan-400/30 rounded-2xl shadow-2xl p-8 flex flex-col justify-between hover:scale-105 hover:shadow-cyan-400/25 transition-all duration-300 mb-4">
                                <div>
                                    <h2 class="text-2xl font-bold text-white mb-3 text-center">
                                        Sol <%= sol %>
                                    </h2>
                                    <p class="text-teal-300 text-center text-sm mb-5">
                                        Season: <span class="font-semibold">
                                            <%= solData?.Season ?? 'N/A' %>
                                        </span>
                                    </p>
                                    <dl class="grid grid-cols-1 gap-3 text-sm text-cyan-100">
                                        <div class="flex justify-between border-b border-cyan-400/10 py-2">
                                            <dt class="font-medium">First UTC</dt>
                                            <dd>
                                                <%= solData?.First_UTC ?? 'N/A' %>
                                            </dd>
                                        </div>
                                        <div class="flex justify-between border-b border-cyan-400/10 py-2">
                                            <dt class="font-medium">Avg Temperature</dt>
                                            <dd>
                                                <%= solData?.AT?.av ?? 'N/A' %> °C
                                            </dd>
                                        </div>
                                        <div class="flex justify-between border-b border-cyan-400/10 py-2">
                                            <dt class="font-medium">Avg Wind Speed</dt>
                                            <dd>
                                                <%= solData?.HWS?.av ?? 'N/A' %> m/s
                                            </dd>
                                        </div>
                                        <div class="flex justify-between border-b border-cyan-400/10 py-2">
                                            <dt class="font-medium">Avg Pressure</dt>
                                            <dd>
                                                <%= solData?.PRE?.av ?? 'N/A' %> Pa
                                            </dd>
                                        </div>
                                        <div class="flex justify-between py-2">
                                            <dt class="font-medium">Wind Direction</dt>
                                            <dd>
                                                <%= solData?.WD?.most_common?.compass_point ?? 'N/A' %>
                                            </dd>
                                        </div>
                                    </dl>
                                </div>
                            </div>
                            <% } %>
                    <% } %>
                </div>
            </div>
        </main>
</body>
<%- include("partials/footer.ejs") %>

</html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>NASA Mars Weather</title>
    <link rel="icon" type="image/png" href="/images/logo.png">
</head>

<body class="bg-gray-900 min-h-screen flex flex-col">
    <%- include("partials/header.ejs") %>

        <main class="flex-1 px-4 py-8">
            <div class="text-center mb-12">
                <h1 class="text-5xl md:text-6xl font-bold text-white mb-4">
                    Mars Weather
                    <span class="bg-gradient-to-r from-red-400 to-orange-500 bg-clip-text text-transparent">
                        Data
                    </span>
                </h1>
            </div>
            <div class="flex justify-center <%= locals.sols ? 'hidden' : '' %>">
                <div class="pt-8">
                    <form action="/mars-weather" method="POST" class="space-y-6">
                        <button type="submit"
                            class="w-full bg-gradient-to-r from-red-600 to-orange-600 hover:from-red-700 hover:to-orange-700 text-white font-bold py-4 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
                            Fetch Latest Weather
                        </button>
                    </form>
                </div>
            </div>

            <div class="max-w-7xl mx-auto px-4">
                <div class="grid grid-cols-2 gap-6">
                    <% if (locals.sols) { %>
                        <% for (var i=0; i < locals.sols.length; i++) { %>
                            <% const sol=locals.sols[i]; const solData=locals.weather[sol]; %>
                                <div
                                    class="bg-gradient-to-br from-gray-800 to-gray-900 p-6 rounded-2xl shadow-2xl border border-gray-700 mb-8">
                                    <h2 class="text-xl font-bold text-white mb-6 text-center">
                                        Sol <%= sol %>
                                    </h2>
                    
                                    <div class="mb-6 p-4 bg-gray-800 rounded-lg shadow text-white">
                                        <p>Season: <%= solData?.Season ?? 'N/A' %>
                                        </p>
                                        <p>First UTC: <%= solData?.First_UTC ?? 'N/A' %>
                                        </p>
                                        <p>Average Temperature: <%= solData?.AT?.av ?? 'N/A' %> °C</p>
                                        <p>Average Wind Speed: <%= solData?.HWS?.av ?? 'N/A' %> m/s</p>
                                        <p>Average Pressure: <%= solData?.PRE?.av ?? 'N/A' %> Pa</p>
                                        <p>
                                            Most Common Wind Direction:
                                            <%= solData?.WD?.most_common?.compass_point ?? 'N/A' %>
                                        </p>
                                    </div>
                                </div>
                                <% } %>
                        <% } %>
                </div>
            </div>
        </main>
</body>
<%- include("partials/footer.ejs") %>

</html>